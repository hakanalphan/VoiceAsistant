# Flask Web Framework
Flask
flask-cors
Werkzeug
gunicorn

# OpenAI API (Whisper + GPT + TTS)
openai

# Basic Dependencies

# Audio format support (optional but recommended)
pydub
together

# Env management
python-dotenv

# Python 3.13 compatibility
setuptools
wheel
